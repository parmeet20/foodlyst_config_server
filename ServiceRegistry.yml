spring:
  application:
    name: ServiceRegistry

eureka:
  instance:
    hostname: eureka-server
  client:
    register-with-eureka: false
    fetch-registry: false
management:
  tracing:
    sampling:
      probability: 1.0
server:
  port: 8761
